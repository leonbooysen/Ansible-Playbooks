---
- name: Create New Users
  hosts: all
  tasks:
    - name: Create Users Task
      user:
        name: bob
        state: present
        password: "{{ 'Sp!nkS105' | password_hash('sha512', 'A512') }}"
        shell: /bin/bash
